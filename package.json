{
  "name": "nexus-crm",
  "version": "0.1.0",
  "private": true,
  "description": "Локальная CRM для приёмной комиссии: лиды, ДОД, скоринг, задачи",
  "scripts": {
    "setup": "npm --prefix server install && npm --prefix client install",
    "dev": "concurrently -n server,client -c blue,green \"npm start --prefix server\" \"npm run dev --prefix client\"",
    "build": "npm --prefix client run build",
    "start": "node server/src/index.js"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
